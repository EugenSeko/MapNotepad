<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MapNotepad.Views.MapPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:MapNotepad.Controls"
    xmlns:local="clr-namespace:MapNotepad.Controls;assembly=MapNotepad"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    Title="Maps">
    <ContentPage.Content>

        <Grid
            HorizontalOptions="Fill"
            RowDefinitions="0.07*,0.93*"
            VerticalOptions="Fill">
            <local:SearchBar Grid.Row="0" />

            <Grid Grid.Row="1">
                <controls:CustomMap
                    Grid.Row="0"
                    Grid.RowSpan="3"
                    MapClickedCommand="{Binding ClearClickCommand}"
                    PinClickedCommand="{Binding PinClickCommand}"
                    PinSource="{Binding PinList}" />
                <ListView
                    Grid.Row="0"
                    BackgroundColor="White"
                    IsVisible="{Binding ShowCurrentPinList}"
                    ItemsSource="{Binding ObserPinList}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <ViewCell.View>
                                    <StackLayout>
                                        <Label Text="{Binding Label}" />
                                    </StackLayout>
                                </ViewCell.View>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackLayout
                    Grid.Row="2"
                    BackgroundColor="White"
                    IsVisible="{Binding ShowCurrentPinDescription}">
                    <Label Text="{Binding CurrentPin.Label}" />
                    <BoxView
                        Margin="16,8"
                        BackgroundColor="Black"
                        HeightRequest="1" />
                    <StackLayout Orientation="Horizontal" Spacing="6">
                        <Label Text="{Binding CurrentPin.Latitude}" />
                        <Label Text="{Binding CurrentPin.Longitude}" />

                    </StackLayout>
                </StackLayout>
            </Grid>

        </Grid>
    </ContentPage.Content>
</ContentPage>